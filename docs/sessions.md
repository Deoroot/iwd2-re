# Session ledger

**The last session is the top row.** Several sessions land on the same day, so
the date does not identify one — the tag does.

One command, no reading required:

```bash
git tag -l 's[0-9]*' --sort=-v:refname | head -1     # -> the latest session tag
git log --oneline s28..s29                           # -> everything session 29 did
git rev-parse 's29^{commit}'                         # -> its final commit
```

Note the `^{commit}`: these are ANNOTATED tags, so a bare `git rev-parse s29`
returns the tag object's sha, not the commit's, and will not match `HEAD`.

There is deliberately no commit column below: the tag *is* the commit, and a
column holding it could only ever be one commit stale — the row naming a hash
has to be written before the commit that contains it exists.

Each session ends by tagging its final commit `sNN` and adding a row here.
Numbering matches the `UPDATE NN` blocks in the `iwd2-re-combat-gap` memory
file, which carries the full detail; this table is only the index.

| # | tag | date | pushed | headline |
|---|-----|------|--------|----------|
| 48 | `s48` | 2026-09-06 | no | **The merged picker case of `CInfButtonArray::OnLButtonPressed` is three arms, not one, and all three are now recovered -- together with the 0x73 arm's stealth body, which was the last of s47's priced gaps.** Three commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). **`CItem`'s `pack(2)` was declared skipped at the top of the session** -- sixth session untouched, but said out loud, which s47 did not do. The ten states that shared one paraphrased body are 0x59138F (0x66/0x67/0x68/0x69/0x70/0x71/0x7A), 0x592428 (the innate pickers 0x6A/0x6B) and 0x592858 (the feat-point confirm 0x7B). Each dispatches types 0x15..0x22 through a three-slot table of its own -- 0x593BD8, 0x593C34, 0x593C50, each with a fourteen-byte index table -- and all three were DERIVED with `sym.py u32`/`bytes`, per s45. **The merge was wrong in seven ways, and every one of them is a thing one arm does that another does not.** The shift page step belongs to 0x59138F alone, so the innate and feat-point arms were carrying a branch the binary does not put there. `bUseNow` is (0x67, 0x69, 0x70, 0x7A) in the seven-state arm and plainly `m_nState == 0x6A` in the innate one -- the merged flag had 0x6A in the wrong arm's list. **A NULL picker list and an out-of-range type do NOT pop**: 0x5918DD and 0x592E65 clear the selection and repaint, nothing more, where the merged body fell through to a PopState; and even those two exits differ, one writing `m_nSelectedButton` and the other calling the setter. **The spellbook's CanCast gate is real and had been marked unrecovered**: 0x591502 demands the picked CSpell -- the one-argument `CSpell(CResRef)` inlined, which is `SetResRef(res, TRUE, TRUE)` -- resolves the specialisation mask and on refusal feeds back FEEDBACK_89 with `GetClassBeyondCastingAbilityStringRef`. **The grey-out flag is read through a pointer the PROLOGUE computes**: 0x58FF88 stashes `&m_buttonArray[buttonID] + 0x1DC` and the arm dereferences it twice for two different purposes -- skipping the CanCast gate, and skipping the dispatch -- which is why a customise click can bind a greyed cell without firing it. The innate arm has no grey-out gate at all, which is what the merged body's invented `bInnateArm` exemption was groping at. And **the three tails are three different sequences**, one of which inlines `ClearPickerList` in full -- matching 0x587BD0 instruction for instruction -- while **0x5939DD turns out not to be a SetState at all**: it is the second push of `PopState`, so the innate arm's `push 1; jne 0x5939DD` is `PopState(0, 1)`. **Then the 0x73 arm's type-0x0B body, s47's measured partial, is recovered.** Turning stealth ON builds a `CAIAction` on the stack whose three `CAIObjectType` members come from the eleven-argument ctor called with exactly its defaults -- plain `CAIObjectType()` -- and sets only three fields on top: the action id from the WORD at 0x8477A6, which is 18; `m_specificID2 = -1`; and `m_dest = (-1, -1)`. Every offset was checked against our pack(2) `CAIAction` off a frame the eleven pending pushes shift by 0x2C, and 0x403A40 provably does not touch `m_dest`, which is why this arm has to. Turning it OFF writes the dword at 0x85BD1C -- 90 -- into `m_nStealthGreyOut` at +0x727A and posts item effect 0x88, which is `CGAMEEFFECT_FORCEVISIBLE`. **And the tail at 0x59082D belongs to that type alone, so the `m_nState = 0` it performs was missing rather than shared.** **Measured, not argued: fourteen frames diffed, every one 0 of 168.** A new route drove the ORIGINAL through both picker arms -- `0x6E 0x72 0x75 0x77 0x66 0x72 0x75 0x6B 0x72`, 173 frames -- and ours walked the identical nine-run sequence, all nine at 0 of 168. **The two 0x72 runs that follow a cell click are what prove the `PopState(0, 1)`**: taking the deque's FRONT would have landed on 0x77 and 0x75. New scenario `actionbar-lclick-pickercells.txt`. The skills route then measured the stealth body at four more frames of 0 of 168 -- **but only after a second pairing trap of s43's family was found and fixed**. `--diff --state` reports the LAST frame of a run, and the original's run of 0x72 after the stealth click is 44 frames long with `b3.sel` reading 1 for the first twelve and 0 for the rest, because the action RESOLVING is what clears the selection. Our two-frame run was being compared against a moment 44 frames later. `c:\tmp\s48_cutf.py` generalises s47's cutter one more step -- it truncates by run index AND by frame index within that run -- and against frames 1, 2 and 12 the same comparison is 0 of 168. **Not claimed: that our action resolves the same way.** Holding the scenario longer extends our own walk but our logger emits far fewer frames than the original's, so the late phase has no counterpart to compare; the long hold was reverted. That run did reproduce TAREA 2a independently, on a route that had never shown it: the original goes `0x76 -> 0x67 -> 0x76` and ours goes `0x76 -> 0x72`. **A real bug was caught before it shipped, and only by a structural check.** The first attempt inserted the 0x7B arm in the MIDDLE of the label list, so `case 0x70:` fell through into the feat-point body -- and it compiled and passed `arc check`. A count assert cannot see a correct insertion in the wrong place; the patch script now asserts the label GROUPING, that each arm opens its brace on its last label and no earlier one. The commit was reset and redone as one. Left unrecovered and named: 0x716770, which a shift-click on an unspecialised class-3 spellbook cell calls, walking a two-dimensional resref table off CInfGame +0x145C column-picked by `IcewindMisc::IsEvil`. **And OnLButtonPressed is still NOT complete** -- three ACTION-BAR arms carry `NOTE: unrecovered` from s46 (0x5933C3 quick spell, 0x5935B5 quick innate/feat, 0x59393C quick song), which is a different arc from the state arms this session closed. Parity GREEN on 0x58FF20 after every commit, `arc check` PASS, BOM and CRLF intact, and the marker assert caught **nine** reflowed comments opening with an address -- s45, s46, s47, s48. Jumptable sweep over 515 functions **identical to the s43..s47 baseline on all eleven counters**: CLEAN 442, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, CLONE 3, TABLE COUNT 153, MISSING/DEFAULT/INCOMPLETE/NOT-PARSED 0. |
| 47 | `s47` | 2026-09-06 | yes | **Five of the eight arms `CInfButtonArray::OnLButtonPressed` still owed are now recovered from the binary, four of them measured at 0 of 168 -- and the fifth's one remaining gap is PRICED rather than guessed, at one field of 168.** Nine commits plus this row, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). Pushed: `git ls-remote` reports `s47^{}` = 71354201 on origin, and the row-fixing commit that follows the tag is the only thing after it. The arms: **0x75** (0x592B01, own eight-slot table at 0x593C6C over types 0x23..0x2A) and **0x78** (0x592271); then **0x74** (0x590E26, a BYTE index table at 0x593B64 feeding five slots at 0x593B4C); then **0x77** (0x591A44, eight slots at 0x593C14); then four fifths of **0x73** (0x590556). Every mapping was derived from the tables with `sym.py u32` / `bytes` rather than transcribed, per s45. Three things were wrong in the same way across all of them. **The `m_nCustomizeSlot >= 0 && < 9` guard was invented** on every arm that had one: the binary indexes `m_customButtonTypes` with the raw INT and truncates to BYTE for the sprite, and `SetCustomButtonValue`'s own assert is the only bound in the path -- visible literally in the 0x78 and 0x74 arms, where that assert IS that function's body inlined, same `__LINE__` 2036 and the same two strings as 0x594120's own. **No arm returns to the action bar by name**: every exit that paraphrased as `SetState(0x72, 0)` is the shared repaint at 0x5939CF -- `UpdateButtons`, `ClearPickerList`, `PopState` -- written out at each site because the sharing is the compiler's tail-merge. **And the two exits of the 0x78 arm are not the same call**, which is the whole arm: a type that takes unwinds the entire stack with `PopState(0, 1)` (inlined at 0x59232E as `back()` / `clear()` / `SetState`) while a type that does not steps back one level with `PopState(0, 0)` ten instructions later. 0x5962E0 is the deque's own `erase(begin(), end())` -- a bare `ret`, both iterators read off the object -- which is what makes the first a `clear()` and not a `pop_back()`. The live route proved it independently: the stack there is `[0x75, 0x72]` and `SetState` pushes to the FRONT, so taking the front would have landed on 0x75; the original lands on 0x72. **s34's corollary paid twice more, in two new shapes.** The 0x74 arm's five accepted types are five SEPARATE arms, identical to the instruction -- sixty each, differing only in their own branch targets -- because MSVC does not merge equal switch arms, so five copies is what a source spelling the body out five times compiles to and one case group would have produced one. The 0x77 arm's eight are also separate, and they are NOT interchangeable: each carries its own type as an **immediate**, where 0x74's use the register holding `nButtonType`. Two arms of the same outline, two different things written, and the jumptable audit is blind to both. **The 0x73 body that stood in the source was the ACTION BAR's** -- it cited their addresses, 0x593181 and 0x593295 -- which is s42's corollary in its most literal form: the code was in the wrong function. Checking rather than assuming mattered, because the answer is not uniform: types 4 and 0x77 ARE the same source inlined differently (the action bar calls `GetModalState`, the `CPoint` ctor and the effect's three setters out of line and jumps into the shared tail; this arm inlines all of them), but **type 0x0C is not** -- the action bar's copy leaves `m_iconResRef` alone and this one clears it, the way the GROUP bar's type 8 does. That is the second pair in this file to differ by exactly that one field, after the type-8 pair s46 found. Type 0x0D reads the Animal Empathy rank straight out of `m_derivedStats` rather than through the getter. **Type 0x0B (Stealth) is left partial and says so, with a price.** Its 215 instructions are the arm's other half: both branches of the modal toggle go on to send a message -- a `CAIAction` built from three `CAIObjectType` temporaries into a `CMessageAddAction` (0xE2 bytes, vtable 0x847B40, `m_action` at +0x0C) on the way in, an item effect 0x88 on the way out. It keeps the toggle rather than becoming a stub, because the toggle it performs is faithful and removing it would take working stealth away. Then, per s46, the gap was MEASURED instead of left as a note: driving the new scenario onto that exact button leaves our bar **one field of 168** from the original's -- `m_nSelectedButton` reads 100 there and 5 here, because we set 5 and stop while the original sets 5, sends the action, and the action resolving clears the selection. The other 167 match. **Nineteen frames diffed across three routes, and eighteen are 0 of 168.** New scenarios `actionbar-lclick-customize.txt` (0x6E->0x72->0x75->0x78->0x72->0x75->0x72, 127 original frames), `actionbar-lclick-submenus.txt` (->0x74->...->0x77->, 153 frames) and `actionbar-lclick-skills.txt` (->0x73->, 81 frames); ours walked each sequence identically. The writes are legible in the bar itself: slot 7 of the 0x72 bar goes 5E -> 50 -> 05 on the first route and 5E -> 0B -> 33 on the second, while the skills bar leaves it at 5E, which is what separates 0x73 from its 0x74 twin. `c:\tmp\s47_cut.py` generalises s46's cutter to truncate by RUN INDEX, so `--diff --state` can address the second of three visits to 0x72 rather than only the last. **TAREA 2b is settled and the answer is no divergence**: our harness reached 0x73 on the skills button exactly as Frida's did. It was timing, and it did not reproduce. One naming fix, and it is a warning: `ClearPickerList`'s `RemoveHead` looked wrong against my own accessor notes, and the notes were wrong -- 0x7FBE77 unlinks the node at +4 and relinks through `pNext`, which IS `RemoveHead`, and its mirror 0x7FB343 was an unnamed `FUN_`, now `RemoveTail`. Trusting the note would have turned a faithful line into a wrong one. Jumptable sweep over 515 functions **byte-identical to s43/s44/s45/s46 on all eleven counters** after recovering five arms and adding four switches -- CLEAN 442, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, CLONE 3, TABLE COUNT 153, MISSING/DEFAULT/INCOMPLETE/NOT-PARSED 0. Parity GREEN on 0x58FF20 after every arm, the paired table 0x593C8C still CLEAN 25/25, `arc check` PASS on every commit, BOM and CRLF intact. The patch scripts' own marker asserts caught **five** reflowed comments opening with an address before the lint could -- s45, s46, and now s47. **The `CItem` `pack(2)` was neither done nor declared at the open this time -- fifth session untouched, and s46 at least said so out loud.** Still open in this function: the three picker arms merged into one case (0x59138F, 0x592428, 0x592858) and the 0x73 type-0x0B body. Elsewhere: TAREA 2a (0x76 clicks into `SetState(0x67, 1)` and lands back on 0x72), 0x65 recovered but undriven, 0x68/0x69, 0x70, 0x71/0x7A (needs a bard), 0x7B, and the 0x21 arm. |
| 46 | `s46` | 2026-09-06 | yes | **`CInfButtonArray::OnLButtonPressed` (0x58FF20) holds ONE lock, not nineteen, and its action bar is now recovered from the binary -- plus state 0x76, which the differential caught being a dead stub hours after it was written.** Two commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). The headline is structural: the whole function takes exactly one `GetDeny` on the party leader at 0x590027 and releases it exactly once at 0x593A13, on the single exit every arm jumps to; there is no `GetShare` in its 3,996 instructions. The source had **nineteen** lock acquisitions, one per arm, all on the same leader id -- and carried a comment saying the binary does it the other way. With the sprite hoisted, the state-table default arm (states 0x6F and 0x72, the single-PC action bar) was rewritten rather than patched, per the s40 rule. **Both standing jumptable findings on the paired table at 0x593C8C are gone and it now reports CLEAN, 25 binary arms against 25 source case groups**: the DEFAULT on type 15 was a body the source invented for 0x0F, which the binary sends to a default that does nothing; the SPLIT on 3, 5, 10, 14, 80, 81 and 82 was four `DispatchActionBarClick` types each having a two-instruction body of its own because each passes a literal, while only the quick items pass the runtime value -- merging the seven into one case is exactly what hid it. Six more divergences the audit is blind to: the bounds test, the **live** grey-out gate (unlike its right-button twin, whose chain s44 proved dead -- each range here is a real two-sided test), and a retry loop that spins on SHARED alone with DENIED not in the compare; **the virtual the quick-weapon arm calls through the active engine's vtable slot 0x44 is `GetShiftKey`**, so it is SHIFT-click that opens the weapon picker in state 0x65, and the arm also cleared the modal, readied the slot by mode and re-checked the game state, none of which was here; the cleric DOMAIN button (type 0x39) stores the caster's own `m_specialization` read through the getter at 0x5940D0, where the source wrote a literal 1 that happens to look identical on a cleric whose specialization is 1; types 7, 8 and 0x0C clear the leader's modal unconditionally, and **the type-8 arm here does NOT touch `m_iconResRef` while the group bar's identically shaped type-8 arm does** -- the s34 corollary for the fourth session running; the 0x32..0x38 bank shares a tail that zeroes the level, drops the game state and repaints before pushing 0x67; and the arms ending in the shared tail at 0x5939CF finish with `UpdateButtons` + `ClearPickerList` + `PopState`, not a bare `UpdateButtons`, while the type table's default does NOT walk back to the action bar, which the source invented. States 0x6C, 0x6D, 0x6E and 0x79 now walk the stack back with the inlined `PopState` instead of naming a state, and 0x6E gains its missing button type 6 -- the `SetState(0x6D, 0)` s45 found and could not drive. **Then the measurement earned its keep.** A left-click route over the bar put the ORIGINAL into three states nothing had ever driven -- **0x76, 0x67 and 0x6A** -- and 0x76 was the one the first commit had just left as a deliberate empty stub on "missing is better than wrong" grounds. A plain left click on a Cast Spell button goes there, so the stub was a dead spell-class picker, not a rare path. Its arm at 0x5918F3 is 78 instructions with an eight-entry table of its own at 0x593BF4 over types 0x32..0x39, read with `sym.py u32` rather than transcribed, and it is **not** the action bar's identically shaped bank: it drops the game state by writing `m_nState` where the other calls `SetState`, and its default clears the class and level, tears the picker list down and pops instead of repainting. Measured against the original's own trace of the same route: **0x6E 0 of 168, 0x72 0 of 168, and the new 0x76 0 of 168 -- the fifteenth distinct bar state to measure clean.** Two new scenarios, `actionbar-lclick-bar.txt` and `actionbar-spellclass.txt`. Pairing the 0x72 frames needed the original's trace cut at its first departure from the shared route, because it visits 0x72 three times with three different bars and `--diff` reports the LAST -- the s43 pairing trap, hit again. Left standing and marked in the source: the three picker arms still merged into one case (0x59138F, 0x592428, 0x592858) and the arms for states 0x73, 0x74, 0x75, 0x77 and 0x78 -- their locking is corrected, their bodies are not recovered. Two findings handed forward, both outside this function: our build reaches 0x76 and paints it identically but the click inside it asks `SetState(0x67, 1)` and lands back on 0x72, which points at `SetState` and the picker-list build, not at this arm; and the two harnesses' routes part at the skills button, where `DispatchActionBarClick`'s type-5 arm at 0x5944F5 opens 0x73 only when the leader's modal is zero -- our source already has that gate, so the leader's modal differed between the runs, a timing question rather than a code one. The jumptable sweep over 515 functions is byte-identical to s43's, s44's and s45's on all eleven counters -- CLEAN 442, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, CLONE 3, TABLE COUNT 153, MISSING/DEFAULT/INCOMPLETE/NOT-PARSED 0. `arc check` PASS on both commits, the file's BOM and CRLF survived, and the patch script's own asserts caught four reflowed comments opening with an address before the markers lint could. **The `CItem` `pack(2)` was declared skipped at the top of the session rather than dropped silently -- fourth session untouched.** Still open: 0x65 (recovered, undriven), 0x67 and 0x6A (now known reachable by left click), 0x68/0x69, 0x70, 0x71/0x7A (needs a bard song), 0x7B, the 0x21 arm, and the eight state arms above. |
| 45 | `s45` | 2026-09-06 | yes | **`CInfButtonArray::OnRButtonPressed` (0x594720) is recovered from the binary, and with it the oracle reached THREE more action-bar states -- 0x66, 0x74 and 0x6B -- every one of which measures 0 of 168.** Two commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). The source for it was a paraphrase carrying a `TODO: Incomplete`, so by the s40 rule it was rewritten rather than patched; only the prologue s44 fixed was faithful. **The disassembly has FOUR jump tables, not the two that were known**: 0x595014 with its index table at 0x595028 (the state switch), 0x595034 with its index at 0x595040 (state 0x72's 118 types), 0x5950B8 (state 0x75's six) and 0x5950D0 (state 0x77's eight). `jumptable_audit --multi` pairs all four CLEAN now. The 49 customize types and 8 quick-weapon types were GENERATED out of the binary's own index table instead of retyped, and that mattered: the first hand transcription of that table got two bytes wrong and the cross-check caught them before a line was written. **Six divergences, every one a behaviour change.** The quick-weapon arm at 0x59485C stored into `m_nCurrentSelectedSpellLevel` where the binary stores `type - 0x3C` into `m_nCustomizeSlot`, and it skipped two refusals entirely -- `CheckWeaponUsability` reporting FEEDBACK_ITEMCURSED and a taken `m_items[42]` reporting FEEDBACK_MAGICALWEAPONINUSE. The Cast Spell arm at 0x5948F4 counted casters by `m_nHighestLevel`; the binary counts by list contents and then requires `GetTotalCurrentCount()` before it opens the spellbook. State 0x77's eight arms were `set the class, open 0x66`; each one actually walks that class's levels first, and **Bard and Sorcerer -- the two spontaneous casters -- read `m_nSharedTotal` where the five memorising classes read `m_nSharedCurrent`**, which is the s34 corollary again: eight arms of one shape, two of them different. The domain arm opens on `m_baseStats.m_specialization`, not 0. And the binary takes ONE share and releases it once at 0x594FE1 on every path without ever testing the return code, so the `rc == SUCCESS` guards came out. Measured, not argued: two `frida_orig.py` runs of ten and nine clicks against two new scenarios (`actionbar-spellbook.txt`, `actionbar-skills-innates.txt`), and OUR eleven and ten frames came back in the same order as the original's, state for state. Five states diffed field by field at 0 of 168 on the first route (0x6E, 0x79, 0x75, 0x77 and the new 0x66) and three on the second (the new 0x74 and 0x6B, plus 0x75). **That is fourteen distinct bar states measured, all 0 of 168.** Two things the runs settled for free: right-clicking a type 0x25 slot returns to 0x72 on BOTH sides even though 0x5950B8 sends 0x25 to the default, and the original keeps changing state for a few frames after the last scripted click -- so match frames by (state, types), never by call index, and never read the tail as a result of the last click. **The open 0x6D question is answered**: `sym.py scan 6a006a6d` finds exactly one site, and it is `SetState(0x6D, 0)` -- second argument 0, not 1 -- at 0x5901B5, which is slot 0 of a five-arm table at 0x593A98 switching on `m_buttonTypes[buttonID] - 6` inside `OnLButtonPressed`. Its index table at 0x593AB0 gives type 6, and **no reachable group bar shows a type 6** (0x6E is `07 08 0F 10 11 12 13 14` and four greyed 0x64), which is why nothing had driven it -- the same category as the 0x21 arm and the bard song. Our source has no `SetState(0x6D` anywhere. `OnLButtonPressed` (0x58FF20) is now the obvious next arc and it is a much bigger tangle than this one: ELEVEN jump tables against six source switches, with DEFAULT and SPLIT findings on the big type table at 0x593C8C. The jumptable sweep over 515 functions is byte-identical to s43's and s44's on all eleven counters -- CLEAN 442, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, CLONE 3, TABLE COUNT 153, MISSING/DEFAULT/INCOMPLETE/NOT-PARSED 0. `arc check` PASS, `re-agent parity` GREEN, and the file's BOM and CRLF survived. Still open: 0x65, 0x68/0x69, 0x70, 0x71/0x7A (needs a bard song), 0x7B, 0x67, the 0x21 arm, and the `CItem` `pack(2)` that still hangs startup, untouched for a third session. |
| 44 | `s44` | 2026-09-05 | yes | **`frida_orig.py` drives a RIGHT click now, and with it the oracle reached SIX action-bar states in one session -- 0x6C, 0x75 (in two different layouts), 0x77, 0x78 and 0x79 -- every one of which measures 0 of 168.** Three commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). s43 closed by naming what it could not see: three families of state that our AutoUI could reach and the original could not, so there was no diff for any of them. The missing primitive was one button. **It needed no new machinery.** The engine polls the right button in a block at `CChitin::AsynchronousUpdate:1579` that mirrors the left one at :1537 field for field -- `CheckMouseRButton()`, `GetAsyncKeyState(m_mouseRButton)`, then `OnRButtonDown`/`OnRButtonUp` with `m_ptPointer` -- and `CScreenWorld::CheckMouseRButton` returns TRUE, so a right click is the existing left one with two `mouse_event` flags swapped. It does NOT go through the vtable slot: calling a handler directly deadlocks on the UI manager's critical section, which is why this driver moves a real cursor at all. The desktop's swap-button setting cancels out rather than biting, since `CChitin` sets `m_mouseRButton` to `VK_LBUTTON` when `SM_SWAPBUTTON` is on and `mouse_event`'s RIGHTDOWN is remapped by that same setting. `--click` and `--rclick` share ONE ordered list, which is not a convenience: the customize menu is reached by left-clicking a portrait and THEN right-clicking one of that character's slots, and two `append` dests would lose which came first. **Where the right clicks go was read off `CInfButtonArray::OnRButtonPressed` (0x594720), not off our source, which is a paraphrase with a TODO on it**: 0x5947EF wants a type of 0x10..0x14 and calls `SetState(0x6C, 1)`, 0x594846 stores `buttonID - 3` and calls `SetState(0x75, 1)`. Two runs of five and eight clicks reached 0x6E, 0x6C, 0x72, 0x75, 0x79, 0x77 and 0x78, and our side mirrored both with `scripts/scenarios/actionbar-rclick-pickers.txt` and `actionbar-customize.txt`. **0x6C and 0x79 came back 0 of 168 on the first reading** -- the first independent check on the second switch at 0x58E56D, whose fourteen arms s43 recovered from asm with no way to run any of them. **0x75 came back 3 of 168, and it was s43's lesson again**: s40's per-arm map prints register NAMES, so four arms that store `ebp` into `m_nIconSequence` had read as computed and been left at 0. Each sets its own `mov ebp, 1` as its first instruction -- 0x23 Skills (0x58DDC3, store 0x58DE29), 0x28 Battle Song (0x58E0B8 / 0x58E11E), 0x29 Clear Button (0x58E145 / 0x58E1AB), 0x2A Restore Defaults (0x58E1D2 / 0x58E238). **Four arms, not the three the bar showed**: 0x28 is the bard song, which no reachable bar displays, and it was fixed on its own evidence rather than left to wait for a save. The five arms that store `ebx` (0) were already right. **A behavioural divergence the new primitive found on its own**: `OnRButtonPressed` refused a greyed button and the original does not. The binary tests `m_bGreyOut` at 0x59474C and then runs a type-range chain meant to refuse the click -- and the chain is DEAD. Each range jumps FORWARD over its own upper bound, so the three `jg 0x594784` exits need `type > 0x4E` and `type < 0x46` at once; those three are the only inbound edges to that `return TRUE`, and its fallthrough is unreachable too because `type > 0x76` implies `type >= 0x6E`, already branched away. Proved in a run as well as on paper: portrait 4's slot 7 is type 0x5E with `m_bGreyOut` set and its control active, and `--click 1:4 --rclick 1:13` opens 0x75 on the original where ours stopped at 0x72. What that replaced was an invented exemption for `m_nState == 0x72 && type == 100`, right about the symptom and wrong about the mechanism. Recorded rather than reproduced: the bounds test is `buttonID > 12`, not `>= 12`, so an id of exactly 12 reads one past both arrays -- inside the object, so no fault, and nothing calls it with 12. **Measurements**: 0x6E, 0x6C, 0x72 (both portraits), 0x75 (both layouts), 0x77, 0x78 and 0x79 all **0 of 168**, across three paired runs; parity GREEN on 0x58A340 and 0x594720; `arc check` PASS on every commit; five `auto_start_game` runs verdict PASS with no crash log and three `frida_orig` runs CLEAN, everything killed to 0 processes each time. **Sweep re-run, 515 functions: identical to the s43 baseline on all eleven counters** -- CLEAN 442, MISSING 0, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, DEFAULT 0, INCOMPLETE 0, CLONE 3, TABLE COUNT 153, NOT PARSED 0 -- which is what it should be, since nothing this session changed the shape of a switch: the four fixed arms gained a statement, not a case, and only `OnRButtonPressed`'s prologue moved, not either of its two tables. Release build still stale from s33. |
| 43 | `s43` | 2026-09-05 | yes | **The oracle reaches PICKER states, and with them the SECOND switch -- the one `UpdateButtons` never had. Five distinct bar states now measure 0 of 168 each, 840 fields, and both of the function's jumptables audit CLEAN.** Three commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). s42 left the arc measuring 0 in the only two states it could reach, and named what it could not see: a second switch at 0x58FC28 every one of whose arms is a picker, plus the 0x15-0x20 arm and the arms 0x0B and 0x21. This session reached three more states and spent most of that list. **Getting there.** `frida_orig.py` drives the original with left clicks only, so the route had to be a button that is not greyed and whose `DispatchActionBarClick` arm calls `SetState` with a picker: portrait 4's Skills button (type 5, slot 3, control 9) reaches **0x73**, the skills submenu; portrait 1's Special Abilities (type 0x0A, slot 11, control 17) reaches **0x6A**, the innate picker; and portrait 0's Cast Spell (type 3, slot 4, control 10) reaches **0x76**. Clicking a portrait while a picker is up drops back to that PC's bar, which is what lets one run visit two. Our side mirrors both routes with `scripts/scenarios/actionbar-skills.txt` and `actionbar-pickers.txt`. A throwaway `bar` sweep photographed all six party bars first, and portrait 4's came back identical to the original's -- an independent check on the state machine that cost one run. **Batch 1: five arms 0x72 and 0x73 showed.** s40 recorded the 0x0B arm's `m_nIconSequence` as a COMPUTED value because the per-arm map prints register NAMES; it is not. 0x58B56A loads the literal 1 into ecx and 0x58B56F stores it, ecx only surviving because the two compares after it reuse the register. 0x77 stores the same 1 as a plain immediate. Both had been leaving the field 0. The Special Abilities button (0x0A) and the nine quick-ability slots (0x5A-0x62) share a flag the prologue computes once for the leader at 0x58A7BB -- innates current, shapeshifts total, shapeshifts current, three unsigned `> 0` tests -- and both grey out when it is clear, 0x0A also on `m_disabledSpellTypes[2] == 1` and the quick slots on the resolved entry's own `m_bDisabled`, decided at the END of the arm (0x58DC32) rather than inside the icon branch. Both re-label the button strref 0x9243, "[MAINGUI BUTTON]No Special Abilities". **0x595EB0 is recovered as `CInfButtonArray::IsUsingModalFeat`** -- Power Attack, Expertise, Arterial Strike, Hamstring, Rapid Shot, the five feats `GetFeatRank` keeps a rank slot for, so `> 0` means the modal is running -- and the 0x0A arm then shows the button selected under 0x9B93, "Using Special Ability". It is a member function that never touches its own object, named for what it does: IWD2's modal feats have no BG2 counterpart. **Batch 2 and 3: the second switch, and the arm that made it look optional.** The per-type switch's 0x15-0x20 arm was a three-branch paraphrase that tested `m_nState` for the formation pickers, built FORM0..FORMB from the button type and otherwise walked `g_pButtonArrayPickerList` itself. The binary's arm (0x58E25F) is **nine unconditional stores and two empty cells** -- it reads neither. Everything a picker shows is written afterwards by the switch at 0x58E56D, on `m_nState - 0x65`, index table 0x58FC54: fourteen of its 23 values have an arm and 0x6E, 0x6F, 0x72 and 0x74-0x79 take the default, which is why no previously reachable bar could prove a line of it. **Ten arms walk the picker list and the binary gives each its own body.** They share a shape and differ in more than parameters: **0x65** walks from `GetHeadPosition` with no count cap over all twelve slots and takes its selection from the leader's equipment rather than the entry; **0x73** BAILS above 9 entries instead of paging and starts at slot 5, the five skill buttons keeping 0-4; the other eight page, more than 12 entries meaning slots 1..10 with 0 and 11 left for the arrows. They differ again in fallback resref (STONWEAP / STONSPEL / STONITEM / STONSPEC / STONSONG), in tooltip field (`m_name` for 0x66/0x67, 0x71/0x7A and 0x73, `m_abilityId.m_strDescription` for the rest) and fallback (0x1356, 0x134A, 0x923A, 0x923B, 0x923C), and in whether they write a grey-out at all -- 0x65, 0x68/0x69 and 0x7B do not. Only 0x66/0x67 and 0x7B pass a third argument to `SetToolTipStrRef`. **Two carry a modal-feat chain over SPIN275..SPIN279**: in the innate picker an entry shows selected while any of its spell states is set (`SPLSTATE_FEAT_POWER_ATTACK_1..5`, `EXPERTISE_1..5`, `ARTERIAL_STRIKE`, `HAMSTRING`, `RAPID_SHOT`) and greys out when the leader lacks the feat; in the rank picker 0x7B the list holds one entry per rank and the entry whose `m_count` equals `GetFeatRank` is selected. The two formation arms are one body the compiler duplicated -- identical instruction for instruction bar their temporaries' stack slots -- and are twelve icon cells, FORM0 through FORMB, and nothing else. **The prologue too**: there is no test of the group's count in front of `GetGroupLeader`, the share is taken ONCE rather than retried on SHARED/DENIED, and a share that fails RETURNS, so the rc/pSprite tests the arms still carry are redundant rather than wrong. **`CItem`'s four missing bytes are a VTABLE POINTER, not a bigger `CResHelper`** -- s42's remaining half of that debt, closed. `CItem::~CItem` is virtual: every constructor ends `mov dword ptr [esi], 0x84CED0` (0x4E78C6) and that vtable holds exactly one slot, the scalar deleting destructor at 0x4E78E0. Four independent reads agree -- the constructors' own writes at +4/+8/+0xC, `Demand` and `GetAbilityCount` reading pRes at +8 and cResRef at +0xC and `m_nAbilities` at +0x14, the copy ctor's `lea ebp,[esi+4]` / `lea edi,[ebp+8]`, and `CMessageItem` from outside the class putting `m_item` at 0x0C and the SHORT after it at 0xFA. So `sizeof(CResHelper) == 0x10` gets a third witness, and static_asserts now carry it: `offsetof(CItem, m_nAbilities) == 0x14`, `m_useSound == 0x24`, `sizeof(CSound) == 0x64`. **What is NOT closed is written into the header rather than forced**: sizeof is 0xF0 here against the binary's 0xEE, the two bytes are trailing padding, only `#pragma pack(2)` removes them, and with that pragma the build dies before world activation (exit 0xCFFFFFFF, no crash log) while the same build without it runs clean. Bisected, recorded, left alone. **A tool defect on the way**: `re_lint.py` ran `git diff` through subprocess with `text=True` and no `encoding=`, so deleting one of the two files' pre-existing double-encoded em dashes put a byte cp1252 cannot decode into the diff, the reader thread died, stdout came back None and `arc check` failed with an AttributeError naming nothing -- the same defect s42 fixed in `src_find.py`. **Measurements**: 0x6E, 0x72, 0x73, 0x6A and 0x76 all **0 of 168**, where 0x6A had been 36; parity GREEN on 0x58A340 and 0x595EB0; `arc check` PASS on every commit; four `auto_start_game` runs verdict PASS with no crash log and three `frida_orig` runs CLEAN, everything killed to 0 processes. **Sweep, 515 functions: two numbers moved from the s39-s42 baseline, both the second switch** -- CLEAN 441 -> **442**, UNPAIRED-TABLE 36 -> **35**, TABLE COUNT 154 -> **153**; MISSING 0, AMBIGUOUS-PAIRING 52, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, DEFAULT 0, INCOMPLETE 0, CLONE 3, NOT PARSED 0. Both of UpdateButtons' tables audit CLEAN, 42 of 42 arms and 10 of 10. Release build still stale from s33. |
| 42 | `s42` | 2026-09-05 | yes | **`CInfButtonArray::UpdateButtons` is rewritten for every arm this box can SEE, and both bar states now measure 0 fields differing out of 168 -- down from s41's 34 of 156. The sweep's last two MISSING are closed, and the oracle reaches a second state.** Three commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). s41 left the arc scoped rather than blocked; this session spent it. **Batch 1, the nine arms state 0x6E exercises.** The headline structural fact is that `settings.m_bActive` (+4) and `settings.m_bHasOverlay` (+8) are INDEPENDENT stores, not two faces of one flag -- surveyed over all 43 arms: the overlay arms leave `m_bActive` 0 while `m_bHasOverlay` is 1, the arms carrying their own icon BAM do the reverse, the 0x28 arm (Customize: Battle Song) sets BOTH, and the empty-slot arm clears all three; ours drove all three off one local. **Arms 0 and 1 (0x58A9CA / 0x58AAB8) are recovered** -- the sweep's two MISSING -- and they are two BODIES, not one: they differ only in the GUIBTACT frame, 0x30 vs 0x34, which goes to both the normal and the selected slot, and they converge at 0x58ABA1. Neither sets a tooltip or a hot key. The five formation slots keep their frames at -1 where ours forced 0/0, test their quick-formation against **-1 exactly** and not "negative", and take a hot key of the button TYPE plus 0x20 -- 0x30..0x34, where ours computed 0x20 plus the slot index. The empty slot (type 100 and the shared default at 0x58E4AD) clears `m_bHasOverlay` and `m_bSelected`, greys out, takes tooltip 0xA010 when the ARRAY's state is 0x72 and -1 otherwise, and makes **no SetToolTipHotKey call at all**. Both cells are always written, always with `bSetAutoRequest` and `bWarningIfMissing` TRUE, and `bDoubleSize` is the discriminator over the 62 SetResRef sites: a cell that receives a real resref gets `CBaldurChitin::GetDoubleSize()`, a cell that receives "" gets FALSE. No SequenceSet and no FrameSet -- UpdateButtons never touches a frame cursor. The invented GUIBTBUT / `m_nNormalFrame` / `m_cVidCell` / `SetEnabled` epilogue is **gone**: the binary touches `pButton` through exactly two vtable slots, +0x44 and +0x48, and hands the second the `"F%d"` label the loop head builds, where ours passed an empty string. The shared tail at 0x58E52F is added. **A name in the wrong place was the last thing left differing in 0x6E.** That tail calls 0x5959A0, and 0x5959A0 **is** `CheckActivation` -- BG2's PDB gives `int CInfButtonArray::CheckActivation(long buttonId)` with a `CGameSprite* pSprite` local and sixteen `$LN` jumptable labels, which is 0x5959A0's shape and not 0x594170's. It is recovered: the byte table at 0x595C00 and the jumptable at 0x595B7C map a button type onto its slot in `CDerivedStats::m_disabledButtons` (Stealth 0, Thieving 1, Attack 2, the bard song 0x0C, and the three banks of nine quick slots at 3, 0x0D and 0x1F), and a type with no slot -- plus an empty group and a leader the object array will not share -- answers TRUE. Nothing is greyed unless the sprite says so. 0x594170, which the source had been calling CheckActivation, is the hot-key path out of `CScreenWorld::OnKeyDown` for a button the bar is not showing, and is renamed `ActivateHiddenButton` -- named for what it does, flagged as such. **s41's `CVidCell` lead is REFUTED**, and the oracle was the thing at fault: a CVidCell carries TWO CResHelper resrefs, its own `CResHelper<CResCell>` at +0xAC and `m_header`'s at +0xBC, and the probe was reading the original's first against our second. The inlined SetResRef bodies inside UpdateButtons put the base at cell+0xA4 and m_header at cell+0xB4, which is exactly what our header already says -- CVidCell is NOT a second class with the CItem CResHelper skew. The probe now reads both on both sides, as `res` and `hres`. **A second bar state.** The original has no UI scripting past its load sequence, which is why every measurement so far was of the one state the save opens in -- but `frida_orig.py`'s click machinery is generic, so all that was missing was the world engine's `this` and somewhere to queue jobs once it is up. `--click 1:0` clicks the first party portrait and the bar becomes the single-PC bar, **state 0x72, nine more arms**; ours reaches the same place with `scripts/scenarios/actionbar-states.txt`, and the armed `Iwd2DebugLog` now fires on every change of (`m_nState`, `m_nSelectedButton`), so ONE run per side holds both states and `--state` / `--call` pick between them. 0x72 opened at 25 of 168. It found that the weapon-set arm stores frames 0x68/0x6A and sequence 1 at 0x58CE5D BEFORE looking at the slot, so its empty branch keeps them; that the quick-spell, quick-item and quick-ability arms store -1 into both frame slots while the quick-song arm stores the bard song's own 0x14/0x16, where ours forced 0/0 in all four; that **`m_nIconSequence` is a per-ARM constant, not a per-slot one** -- 1 for the weapon sets and quick items whatever they hold, 0 for the spells, abilities, songs and item usages whatever they hold, and a literal 1 in the arms for types 2, 4, 5, 0x0C and 0x0D; and that an empty quick spell greys out on its count alone (0x58D89D) as does an empty quick item (0x58D519), the latter past an exemption comparing `settings.field_1D4` against strref 0x6097, "Tiernon's Hearthstone" -- **a field NOTHING in the binary ever writes and no constructor initialises**, so the test never fires. Reproduced, not corrected. **A tool defect this uncovered**: `src_find.py` opened source in the console codepage, so a byte with no slot there decoded to U+FFFD and could not be written back to a pipe; `--body` died mid-function and every caller read the function as "could not place", which is why the sweep had been reporting UpdateButtons NOT PARSED rather than auditing it. Two files in `src/` trip it today. Fixed in `src_find.py` and in the three `jumptable_audit.py` call sites plus `arg_provenance.py`. **Sweep re-run, 515 functions, and it is the s39/s40 baseline with ONE number moved: MISSING 2 -> 0**; CLEAN 441, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 36, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, DEFAULT 0, INCOMPLETE 0, CLONE 3, TABLE COUNT 154, NOT PARSED 0. UpdateButtons' own table now pairs all 42 arms CLEAN. Runtime: three `auto_start_game` Prologue runs with the new scenario, all verdict **PASS / 5 steps / 0 failed**, no `iwd2-re-crash.log`, game killed to 0 processes each time; plus two `frida_orig` runs on the original, both CLEAN, both killed to 0. parity GREEN on 0x58A340 and 0x5959A0; `arc check` PASS on every commit. **What the oracle still cannot see**: the second switch at 0x58FC28 after the loop (still the UNPAIRED-TABLE) -- every one of its arms is a PICKER state, so 0x6E and 0x72 both take its default and neither can prove it -- the 0x15-0x20 item-usage arm, whose source is still a paraphrase with branches the binary does not have, and the arms no reachable bar shows. Release build still stale from s33. |
| 41 | `s41` | 2026-09-02 | yes | **The blocker s40 stopped on is gone: there IS an oracle for the action bar on this box, and its first measurement says 34 of 156 fields of `CInfButtonArray`'s output are wrong.** Four commits, tree clean, Debug rebuilt at HEAD (**Release still NOT rebuilt -- stale since s33**). **`CGameArea::FindObjectNear` (0x46DAE0) is recovered**, the last of the four area searches and a stub since forever. BG2's PDB names it as the CPoint overload of `GetNearest` and spells its parameters (`center, type, range, terrainTable, lineOfSight, seeInvisible, nNearest`); IWD2 appends `includeAll` and uses the seventh as a `findFarthest` switch, and `ret 0x24` confirms the nine dwords. It is `GetNearest`'s twin in shape only. Starting from a bare point instead of a searcher object means no searcher type and so no immunity list, and no vert-list anchor either: the walk is the whole of `m_lVertSort` from the head, one direction, no back list. It is the **only one of the four searches that asks the type whether the candidate is inside its location filter**, and it hands `CAIObjectType::IsOver` the PERSPECTIVE-scaled point rather than the raw one. Its own test set: no state mask, no sleeping test, no stealth test; `includeAll` tested against zero where `GetNearest` tests `ignoreDead` against 1 exactly; `CheckLOS` given FALSE for `bCheckIfExplored` where `GetNearest` gives TRUE; and the race 0xBE special case, absent from both ordinal twins, present here. `findFarthest` is compared against 1 EXACTLY, so any other non-zero value keeps neither end and the search answers INVALID_INDEX -- reproduced, not corrected -- and the far-edge break returns the running best rather than failure. **The action-bar oracle exists and works**, which is what s40 said the `UpdateButtons` arc was waiting for. It needed three defects fixed in the original-side driver, none of which named its own cause: `frida_hooks.py` emitted a dump label as an UNQUOTED JS object key, so any label with a dot in it failed the whole script with a bare `SyntaxError: expecting '}'`; its `str` reader was `readUtf8String(64)`, which reads EXACTLY 64 bytes and turned every NUL-padded `CResRef` into a decode error; and `frida_orig.py` calibrated its cursor map from two HARDCODED desktop points, which on a WINDOWED game (this box's sits at 563,230) measured one off-window `(-1,-1)` sentinel against one real reading, produced `ax=0.095`, and missed every aim afterwards. It now sweeps a grid of the desktop, nearest the middle first, keeping the first two REAL readings that differ on both axes; and an aim that reads the sentinel now recalibrates instead of "correcting" itself a further 640px away, which is what killed the click on the arbitration screen that loading a save reaches after the display mode has changed under the stale map. With those three fixed, `frida_orig.py --load-slot 0` spawns `IWD2.exe`, loads the save unattended and reports RESULT: CLEAN. **`scripts/probes/actionbar_settings.py`** is the harness: it photographs the FIELDS `UpdateButtons` wrote -- better than a screenshot -- at the entry of `RenderButton` (0x5950F0), which runs once per slot per frame over exactly that output, so no `onLeave` hook and nothing added to the original. `--load-slot N` and our `--slot N` are the same visible load-screen row, which is what makes the two runs comparable. Two structural corrections fell out of the first run: **`nButton` is `args[3]`, not `args[4]`**, and **`RenderButton` AND `RenderButtonOverlay` are BOTH called for all twelve slots every frame** -- neither is dispatched per arm, so the `m_bActive`/`m_bHasOverlay` pair inside them is what decides what is drawn, not the caller. **First measurement**, Prologue autosave, state 0x6E (the group action bar), identical across all twelve photographs on the original's side: `m_nState`, `m_nSelectedButton` and all twelve button types AGREE, so the state machine is right -- and **34 of 156 fields differ**. Ours sets `m_bActive` on the three overlay arms where the original clears it; leaves `m_nIconSequence` at 0 where the original writes 1 on all eight live slots; leaves the five formation arms' icon frames at 0 where the original writes -1; never gives those five slots their formation resrefs (FORM6, FORM8, FORM1, FORM9, FORM5); and sets `m_bHasOverlay` and `m_bSelected` on the four empty slots where the original clears both. **`UpdateButtons` itself was still not touched, deliberately**: the source is a paraphrase top to bottom, so it is a rewrite and not a patch, and ONE state out of twenty-three is not enough verification to start one. What changed is that the arc is no longer blocked -- it is scoped. **Two old debts closed.** `CAIGroup::GroupSetTarget`'s encumberance-2 arm ends on a jump from 0x406D4B that lands at 0x407122, past the list's `RemoveAll` at 0x407119 -- the only thing between them, and the only jump into 0x407122 in the function. So an overloaded member gives its "too heavy" feedback and walks away, leaving the FACE and MOVETOPOINT it already queued in a list that is a FUNCTION-level local, and the next member consumes them with its own. Ours drained and deleted them. Reproduced, not corrected; the drain loop is gone rather than moved, because the binary has no such loop -- both consumption paths leave the list empty and fall into the bare `RemoveAll`. And the `m_bParsed`-into-`CRes` suggestion is **refuted, with the reason recorded in CRes.h**: BG2's PDB really does give `CResCRE` a field list of one base class and five methods, and BG2's own `CRes` really does end with `bWasMalloced`/`bLoaded` -- but that is the name carrying over, not the slot. In IWD2, `CResItem`, `CResSpell`, `CResUI`, `CResWED`, `CResTile`, `CResPLT` and `CResBinary` all put real DATA at 0x50 and `CResBitmap` puts three fields there before its own flag reaches 0x58, so a BOOL in `CRes` would shove all of them along four bytes. `CRes` ends at 0x50; each subclass pays for its own flag. New accessory: **`CAIObjectType::IsOver` = 0x40D920**. A lead, not a verdict, for the `CItem`/`CResHelper` debt: the binary keeps a `CVidCell`'s resref at +0xAC while our header puts `m_header.cResRef` at +0xBC, a 0x10 gap in a SECOND class. Runtime: one `auto_start_game` Prologue run with `c:\tmp\s35_walk.txt`, verdict **PASS / 15 steps / 0 failed**, no `iwd2-re-crash.log`, game killed to 0 processes; plus three `frida_orig` runs on the original, the last CLEAN with RenderButton x12. **No-regression for `FindObjectNear`** (nothing in a Prologue asks for a location-filtered search), but the relocated `RemoveAll` is on the path a group move actually runs. Sweep deliberately NOT re-run: `FindObjectNear` has no switch and `GroupSetTarget`'s is untouched, so the s39/s40 baseline stands -- MISSING is still 2, and still `UpdateButtons`' arms 0 and 1. |
| 40 | `s40` | 2026-09-01 | yes | **`CAIObjectType::Decode` is COMPLETE -- 32 of 32 arms -- because the two functions blocking it were recovered first; and `CMessageSetVariable::Run` closes the class s38 left half done on purpose.** Three verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. **`CGameArea::GetNthNearest` (0x46BAD0) and `FindNthObjectNear` (0x46DDC0)** replace their twins' single running best with a distance-sorted insertion buffer of `nNearest + 1` entries and answer the slot asked for, stepping the ordinal DOWN while that slot is still INVALID_INDEX -- so a request for the tenth nearest in a room holding three answers the third. Twelve slots each, which is the width the binary's frame reserves and therefore the real cap. s34's lesson held again: `GetNthNearest` has NO location-filter branch, so a type carrying one is searched from the caller's own position instead of being handed off; it does not special-case race 0xBE; a candidate that has left the vert list is skipped WITHOUT ending that direction, where `GetNearest` ends it; the two-direction walk is two separate bodies rather than one with a toggle; and its `terrainTable` assert is CGameArea.cpp:1188, not AreaRegion.cpp:852. `FindNthObjectNear` drops the state mask, the immunity list, stealth mode and the race test altogether, passes FALSE for `bCheckIfExplored` where its sibling passes TRUE, admits an EQUAL distance where its sibling rejects it, and runs the LOS probe INSIDE the insertion scan rather than before it. **BG2's PDB names neither** -- it has two `CGameArea::GetNearest` overloads, the `startObject` one and a `CPoint` one, and their `unsigned char nNearest` parameter is where the IWD2 pair inherited the name; both IWD2 twins are additions, so these names are descriptive. **Decode's last two arms** then went in behind one shared helper, `SearchAreaNth`, covering all four inline sites (the EnemyOf pair jumps straight into the plain family's bodies, which is why there is one helper and not two). `SecondNearestEnemyOf..TenthNearestEnemyOf` (29-37) run `NearestEnemyOf`'s cutoff and then ask for `id - 28`, so 29 asks 1 and 37 asks 9 -- correct -- and unlike `NearestEnemyOf` this arm has NO side effect on failure. **The plain family carries a real bug in the original**: the arm computes `id - 38`, zero for `Nearest`, and then increments it for BOTH, so `Nearest` and `SecondNearest` ask for the same ordinal 1 and `Nearest` resolves to the SECOND nearest whenever the area holds two matches. Reproduced, not corrected; the ordinal walk-down inside the search is what keeps it harmless with a single match. Jumptable audit on 0x40B880 now CLEAN, 54 case groups against 54 values. **`CMessageSetVariable::Run` (0x50A820)** is NOT the trigger's scope walk with the store swapped in, and s39's four Global findings were measured against it rather than assumed: the scopes are compared with `_mbscmp` against plain literals, not CString equality against a constructed temporary; the name is neither upper-cased nor split, because it arrives split; **GLOBAL is not exclusive** -- a message naming GLOBAL writes the game store and then falls into the LOCALS-or-area walk as well, so a variable called GLOBAL is written twice; the GLOBAL store runs even when the deny was never granted, while everything past it and the ReleaseDeny are gated on `rc == SUCCESS`; and LOCALS on a non-sprite writes nothing and answers nothing. The named-creature form comes FIRST and short-circuits the rest: with an object denied and bit 2 or bit 4 set in `field_001A` it REMOVES that creature's entry from the area's name store, the game's, or both, and returns -- which is what those two bits are for. Reproduced as they stand: no null-check on the denied object before MYAREA reads its area pointer (it works only because an area's name sits at offset zero, the same identity s39 found behind `GetHeader`); the increment flag tested against 1 exactly, so any other non-zero value overwrites instead of adding; and every store allocating a whole `CVariable` with `new`, filling it, handing it to `AddKey` and deleting it again. **`CInfButtonArray::UpdateButtons` (0x58A340) was measured and deliberately NOT touched: it is an arc, not a two-arm patch.** Its two MISSING arms cannot be expressed in the current source at all, because that source is a paraphrase from top to bottom -- the prologue invents a GetCount check and a GetShare retry loop the binary does not have; every one of the 42 arms writes `settings` and `pButton` DIRECTLY, so there is no local-variable epilogue to add a case to; the invented epilogue's GUIBTBUT / `m_nNormalFrame` / `SetEnabled` block does not exist in the binary at all, which touches only vtable +0x44 and +0x48; the shared tail is just `if (field_0 && !m_bGreyOut) m_bGreyOut = !CheckActivation(type)`; and there is a whole SECOND switch after the loop, on `m_nState - 0x65` over 23 values through the table at 0x58FC28, that the source does not have -- that is the UNPAIRED-TABLE, not an inlined callee. Arms 0 and 1 are one body differing only in `0x30` vs `0x34` for both icon frames. **The blocking unknown is `m_bActive`**: the binary writes 0 to it in 38 of 43 arms and our `RenderButton` bails out when it is 0, which is faithful -- the overlay arms are drawn by `RenderButtonOverlay` instead, both reached from the same `FUN_0077B530`. So the arc needs UpdateButtons, RenderButton and RenderButtonOverlay measured together, plus a way to SEE the action bar; neither exists in this box's setup, and a half-verified rewrite of the most visible UI in the game is exactly what "missing better than wrong" forbids. The full per-arm map is `c:\tmp\s40_map.py` -> `c:\tmp\s40_arms.txt`. **Sweep re-run, 516 functions: identical to s39's baseline on all ten counters** -- CLEAN 441, MISSING 2, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 36, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, DEFAULT 0, INCOMPLETE 0, CLONE 3, TABLE COUNT 154 -- so the s38-to-s39 drift did NOT recur and that baseline is stable. Both MISSING findings are UpdateButtons' arms 0 and 1. Runtime: two `auto_start_game` Prologue runs with `c:\tmp\s35_walk.txt`, both PASS / 15 steps / 0 failed, no `iwd2-re-crash.log`, game killed to 0 processes each time -- **no-regression for Decode, not proof of the new arms**, since nothing in a Prologue asks for an Nth-nearest, and `Run` is multiplayer-only so no single-player run reaches it. Release build still stale from s33. |
| 39 | `s39` | 2026-08-31 | yes | **`CAIObjectType::Decode` (0x40B880) went from 17 of its 32 arms to 30, the sweep's last MISSING is closed, the `Global`/`GlobalGT`/`GlobalLT` SPLIT is gone -- and for the first time in five sessions a live run PROVED the new code ran rather than merely not regressing.** Three verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. Decode was not a switch with holes, it was a switch with holes on top of a paraphrased scaffold, so it was rewritten whole from the two tables (0x40C934 / 0x40C9B8, 255 values from 1) with the OBJECT.IDS names read out of the game's own `Data/IDS.bif`. **Five scaffolding facts the missing arms had nothing to do with.** `default` is not "resolve to NOONE and give up": it resets the working type to the CALLER's own AI type and carries on round the chain, which is where `GroupOf`, `LastHitter` and every id past `SpellTarget` bar `Nothing` land. `Myself` neither shares nor releases -- it adopts the caller only when the location filter admits it and otherwise leaves the working type untouched. The name lookup UPPER-CASES the name before hashing, and a named creature whose variable holds a NEGATIVE id is not an error: the special-case chain still runs, unresolved. There is no `caller == NULL` guard in the binary. The stored-type arms copy the field BEFORE releasing the object it lives inside, and every failed lookup resolves `*this` to NOONE while still holding its share. **The SPLIT s38's sweep flagged inside Decode is real and its point is one missing test**: the source merged 2 / 21-26 / 27, the binary gives them three bodies, and `LeaderOf` is the one that does NOT re-test the location filter after the lookup. New arms: `LeaderOf`, `NearestEnemyOf`, `Player1..6`, `Protagonist`, `Farthest`, `FarthestEnemyOf`, `NearestPC`, `NearestEnemySummoned` and `Nothing`, plus the seven party-criteria arms below. Only `NearestEnemySummoned` tests the id its area search returned against INVALID_INDEX; `Farthest`, `FarthestEnemyOf` and the Nth-nearest family adopt it unchecked and let the following lookup fail instead. `NearestEnemyOf` has a side effect on failure: an evil sprite that came up empty clears CRE flag 0x10000 and stops hunting. **`CInfGame::GetCharacterCriteria` (0x5BDFE0) is recovered, and BG2's PDB named it** -- parameter `criteria` verbatim, locals `sprite` and `characterClass`, and its seven `$LN` labels are exactly the seven arms of the table at 0x5BE820. All seven are the same walk over the portrait-ordered party -- share, skip if STATE_DEAD, score, keep the best id -- differing only in the score: level for `WeakestOf`/`StrongestOf`/`StrongestOfMale` with ties broken on `GetClassMaskLevel(0xD1)` (barbarian, fighter, paladin, ranger) and a `SEX_MALE` test read off the sprite's own AI type for the third; `m_nMaxHitPoints - m_baseStats.m_hitPoints` for the damage pair; `GetAC()` seeded at -20 and 20 for the armour pair, neither of which has a tie-break. A share that never succeeds is skipped WITHOUT a release, and the member id is re-derived through `GetCharacterId` at every single use -- which matters, because that re-reads `m_nCharacters` each time. **The `Global` trio (0x453CE8 / 0x453EF9 / 0x45411E) is three bodies and not one**, the way the four `GlobalTimer` arms below it already are; 0x45411E's apparent differences are inlining only (it CALLS `GetString1`, `GetLength` and `operator==`, and 0x45B220 is a thunk into the same `operator=` body the other two reach at 0x7FCD57). Splitting them cost nothing and re-reading them found four real divergences that predate s34: the name is UPPER-CASED before it is split and BOTH halves come off the upper-cased copy; **LOCALS on a non-sprite returns FALSE** rather than falling through to a null hash and a comparison against zero, and it tests the object type for EQUALITY -- the note at the timer arms claiming Global masks it is wrong; nothing null-checks `m_pArea` before MYAREA reads its header, because `GetHeader` is an identity thunk and `CString::operator=(LPCTSTR)` turns the null into an empty scope, so the invented guard left the scope as the literal `MYAREA` instead; and the scope is compared against a plain literal, not a constructed `CString` temporary. **Runtime, and this is the part that is different from s34-s38**: 0x400F, 0x4034 and 0x4035 ARE among the fifteen ids a Prologue evaluates, so the split arms are reachable from the only save there is. A one-shot armed `Iwd2DebugLog` at each arm plus one per distinct Decode special case, added on a clean tree and stripped with a single `git checkout`, produced **`arm GLOBAL`, `arm GLOBALGT`, `arm GLOBALLT`, and Decode cases 1, 12, 18 and 27** -- so all three Global bodies and four Decode arms ran, two of them (`NearestEnemyOf`, `Protagonist`) newly recovered this session. Three `auto_start_game` Prologue runs, all PASS / 15 steps / 0 failed, no `iwd2-re-crash.log`, game killed to 0 processes each time. The party-criteria arms did NOT run: nothing in the Prologue asks for a WeakestOf. **Sweep re-run, 516 functions: CLEAN 441, MISSING 2, AMBIGUOUS-PAIRING 52, UNPAIRED-TABLE 36, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 9, SPLIT 6, DEFAULT 0, INCOMPLETE 0, CLONE 3, TABLE COUNT 154.** Decode is CLEAN and the Global SPLIT is gone, but more numbers moved than those two changes explain, so treat the s38 column as the stale one and this as the measurement. **The MISSING is now a different function: `CInfButtonArray::UpdateButtons` (0x58A340) has 42 real arms against 40 source case groups, and arms 0 (@0x58A9CA) and 1 (@0x58AAB8) have no case** -- that is the top actionable target. After it, the only ids Decode still lacks are the Nth-nearest family (`Nearest`, `SecondNearestEnemyOf..`, `SecondNearest..`), blocked on 0x46BAD0 and 0x46DDC0, two unrecovered near-twins of `GetNearest` and `FindObjectNear`; the sequence numbers they pass are off by one against the OBJECT.IDS names, `SecondNearest` asking for the same ordinal as `Nearest`. `CMessageSetVariable::Run` (0x50A820) is still not declared. Release build still stale from s33. |
| 38 | `s38` | 2026-08-31 | yes | **`CGameAIBase::EvaluateStatusTrigger` is COMPLETE: 160 of 160 arms, all 164 opcodes with a body. The arc s34 opened at 17 arms is closed, and the jumptable sweep that had been frozen for four sessions was finally re-run.** Three verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. Recovered: `CGameArea::GetNearestDialogMatch` (0x46C460) and the `NearbyDialog` arm; `CVariable::SetIntValue` (0x427860), the whole new class `CMessageSetVariable`, and the `TriggerSetGlobal` arm; the last opcode 0x4092 with a new `GetDistanceSquared` helper (0x45B300). **s34's lesson held to the very end, and it landed hardest on the THIRD list walk.** `GetNearestDialogMatch` is `GetNearest`'s shape and not its code: it builds a `CAIObjectType`, fills it from the caller's AI type and then never reads it again; it seeds the front vert list from the caller's own node when the caller is on that list and from the list HEAD when it is not, walking one node backward and one forward per round; and **the back-list walk is a third test set, not a repeat of the front one** -- it checks `GetVertListPos` and the distance BEFORE `m_active`/`m_activeAI` and never tests `m_activeImprisonment` at all, where the front walk tests all three activity flags first. A match is a sprite whose `m_dialog` OR whose `field_56E4` equals the resref. BG2's PDB names the function and spells its three parameters verbatim (`startObject`, `file`, `range`), which also settles that `range` is a LONG, not the SHORT the prior session had measured. `NearbyDialog` reaches the range through FOUR separate `GetArea()->GetInfinity()` calls while handing the search `m_pArea` directly. **`CMessageSetVariable` needed `#pragma pack(2)`**: its seventh field is a LONG at an offset that is only 2-aligned, so without it the tail 4-aligns and the class measures 0x20 instead of 0x1E -- the `static_assert` on `sizeof` is the measurement, the same recipe that measured `CAreaFileHeader` in s37. Which CString is which is settled by the CONSTRUCTOR, not by declaration order: it assigns +0xC from the SECOND argument, so +0xC is the scope and +0x10 the variable name, and MarshalMessage puts the name on the wire first. IWD2 adds a seventh constructor argument BG2 does not have; it lands at +0x1A, `Run` tests it bitwise, and it stays `field_001A` rather than get a guessed name. **`Run` (0x50A820) is deliberately NOT declared** -- ~1200 bytes with its own three-scope walk plus named-creature bookkeeping, and a guessed body would be worse than the inherited one. `TriggerSetGlobal` splits the name the fixed six-character way, not at a colon like BitGlobal; re-fetches every store for the `AddKey` instead of reusing the `FindKey`'s; replaces `MYAREA` with the area's own header name BEFORE the lookup, and that replaced scope is what the message carries; and answers TRUE on every path, including the two that write nothing. **The last opcode, 0x4092, carries a real bug in the original.** It gates on three squared distances while they are still squared, then square-roots all three and squares them straight back up in the law of cosines; the operand orders across three INTERLEAVED cdecl calls sharing one `add esp, 0x30` were resolved by tracking esp instruction by instruction, which is why s37 left it alone. Its radians-to-degrees conversion is written `angle * 360 / 2 * PI`, and with no parentheses round the `2 * PI` that evaluates as `angle * 180 * PI` -- the binary proves it, multiplying by 360.0, DIVIDING by 2.0 and then MULTIPLYING by pi before comparing against 30.0 -- so the cone the trigger really admits is about three degrees wide, not thirty. The pi constant at 0x847B70 is not pi: it is the literal `3.1415926535` byte for byte, the same truncation `CGameSprite.cpp` already used. **The sweep re-run (515 functions, the first since s33) moved exactly one number: MISSING 2 -> 1**, everything else identical (CLEAN 438, AMBIGUOUS-PAIRING 50, UNPAIRED-TABLE 35, NO-SWITCH-IN-SOURCE 22, OUT-OF-RANGE 10, SPLIT 8, DEFAULT 0, INCOMPLETE 0, CLONE 3, TABLE COUNT 151). Two new facts came out of it: 0x453840's table now pairs UNIQUELY with its source switch (`TimeOfDay`'s second table is the one still reported UNMATCHED, which is the tool under-reporting exactly as s35 documented), and it reports **SPLIT on 0x400F/0x4034/0x4035** -- the `Global`/`GlobalGT`/`GlobalLT` group, recovered long before s34, merges three cases the binary gives three DIFFERENT bodies (0x453CE8, 0x453EF9, 0x45411E), and the tool byte-compared them and did NOT call them clones. That is the top actionable target now, together with the surviving MISSING: **`CAIObjectType::Decode` (0x40B880) reaches only 17 of its 32 arms.** Runtime: one `auto_start_game` Prologue run with `c:\tmp\s35_walk.txt`, verdict **PASS / 15 steps / 0 failed**, no `iwd2-re-crash.log`, game killed to 0 processes -- **a no-regression result, not proof of the new code**, since an idle-or-walked Prologue evaluates only fifteen ids and none of the three new opcodes is among them. Release build still stale from s33. |
| 37 | `s37` | 2026-08-31 | yes | **`CGameAIBase::EvaluateStatusTrigger` went from 140 arms to 157 of 160 -- 161 of the 164 opcodes now have a body, and THREE are left -- plus one whole new function, `CGameContainer::CountItemsExcluding` (0x481C50), and a layout fix in `CAreaFileHeader`.** Seven verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. The map was again NOT re-derived: `s34_map.py` and `s34_sizes.py` in `c:\tmp` still print it, and re-running the size-ranked list after each commit was the whole planning loop. Recovered: LOS and Detect; IsOverMe and InParty; BitGlobal and GlobalBitGlobal; CheckAreaDiffLevel and the unnamed 0x4090; the six TotalItemCnt arms with the container counter behind them; HelpEX and AnyPCSeesEnemy; and the three `NumCreatures*MyLevel` opcodes that share one body. **s34's lesson held again, this time hardest on the object SEARCHES and on the count arms.** `LOS` resolves with `GetObjectTypeChecked` where `Range` beside it uses `GetObjectWithType`, and the explored-terrain flag it hands `CheckLOS` is the TARGET's `Orderable()` state. `Detect` splits on whether the cause names an instance and the two halves share almost nothing: with no instance it SEARCHES the area for the type, shares nothing, remembers an ANONYMOUS type carrying only the found id, and posts the TRIGGER's cause type; with one it resolves the object, range-checks against the square of `GetVisualRange()`, and posts the OBJECT's type. `IsOverMe` releases by the SEARCH id, not by `pObject->GetId()`, and posts `CAITRIGGER_NO_TRIGGER` where Detect posts `See`. `InParty` runs three searches in order -- a named instance by portrait, then every portrait slot by type, then the character OVERFLOW list -- and its named branch never even looks at the share's return code. Findings beyond the arms: **`BitGlobal` and `GlobalBitGlobal` split the variable name a THIRD way**, neither `SplitScriptVariableName`'s fixed six characters nor the timer family's String2, but the whole string upper-cased and cut at the first COLON; **BitGlobal takes its mode from Int1 and its mask from Specifics while GlobalBitGlobal takes the mode from SPECIFICS and the mask from a second variable**, and every way String2 can fail to resolve leaves that mask at zero instead of failing the trigger. **In both, the mode gate is a HARD gate: anything but mode 1 returns FALSE at the first test, which makes the OR and XOR arms unreachable** -- written out anyway because the binary really does test for them in that order. **`TotalItemCnt` and `TotalItemCntExclude` are not the same code under one shape**: the plain triple filters the two fist names and the 0x40-of-0x44 file flags out of the sprite walk and reads the equipment pointer once per slot, the Exclude triple drops BOTH filters -- so a creature's fists DO count there -- and re-fetches the equipment pointer at every single access; and **only `TotalItemCntExcludeGT` passes the exclusion through to the container counter** (0x45972E copies String1 where its two siblings push the empty literal). **The three `NumCreatures*MyLevel` opcodes share one body whose compared quantity depends on SPECIFICS**: zero means the NUMBER of creatures found, anything else the SUM of their levels. `HelpEX` WIPES each `m_lHelp` field back to 0xFF after reading it, and its Class arm ANDs two masks instead of comparing them. `AnyPCSeesEnemy` reads nothing from the trigger and decodes nothing against this object -- it re-Decodes a bare "any enemy" type with each party member as the caller and asks `CanSee` of the MEMBER. **`CAreaFileHeader::field_4C` is three BYTEs and a pad, not a dword**: `CGameArea::Unmarshal` already rebuilt the first two by hand with shifts, and the binary loads them as bytes (0x4734BD, 0x4734D8), stores the third as a byte (0x4735EE) and reads all three as bytes in CheckAreaDiffLevel -- now `m_nMinimumPartyLevel` / `m_nMaximumPartyLevel` / `m_nAveragePartyLevel`, with static_asserts pinning the offsets and the size, and Unmarshal parity still GREEN. 0x4090 is labelled `CAITRIGGER_INPARTYSLOT` for what its body does; unlike `IsPlayerNumber`, which reads the same fixed-order array, its slot is RAW -- no one-based adjustment and no range check. **The three opcodes still open are each blocked on their own prerequisite, named here so the next session does not re-derive them**: `NearbyDialog` (0x459A38) needs `0x46C460`, a ~1400-byte `CGameArea` dialog search taking `(LONG callerId, CResRef, SHORT range)` and answering an object id; **`TriggerSetGlobal` (0x459EAF) needs a CMessage subclass that does not exist in the source yet** -- vtable 0x84882C, ctor 0x45B890, `Run` 0x50A820, 30 bytes, two CStrings -- plus a `CVariable::SetIntValue` for the folded 0x427860; and 0x4092 (0x4576DD) is a float triangle test over three squared distances and a `sqrt`, deliberately left alone rather than guessed. Runtime: one `auto_start_game` Prologue run with `s35_walk.txt`, verdict **PASS / 15 steps**, no crash log, game killed to 0 processes -- **a no-regression result, not proof of the new code**, since an idle-or-walked Prologue evaluates only fifteen ids and none of the 19 new opcodes is among them. Sweep deliberately not re-run: only this function moved on the switch side and it is still short of 160. Release build still stale from s33. |
| 36 | `s36` | 2026-08-30 | yes | **`CGameAIBase::EvaluateStatusTrigger` went from 111 arms to 140 of 160 -- 142 of the 164 opcodes now have a body, and 22 are left -- plus one whole new function, `CGameAIBase::GetReaction` (0x45D560), whose name BG2's PDB confirms.** Seven verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. The map was again NOT re-derived: `s34_map.py` and `s34_sizes.py` in `c:\tmp` still print it, and re-running the size-ranked list after each commit was the whole planning loop. Recovered: the NumItems and NumItemsParty triples; six item-search arms (HasItem, Contains, HasItemInSlot, ChargeCount, PartyHasItem, ItemIsIdentified); the party-level family (CheckPartyLevel, the PartyLevelVS triple, CheckPartyAverageLevel); the four global-timer arms; GetReaction and the Reaction triple; Frame, OnIsland and CurrentAreaIs; PCInStore and AnyPCOnMap. **s34's lesson held again, hardest on the party walks: five of them are in this session and NO TWO share a protocol.** NumItemsParty, PartyHasItem, CheckPartyLevel and the PartyLevelVS triple abort the whole trigger when a share cannot be taken; ItemIsIdentified and AnyPCOnMap SKIP that member instead; and PCInStore does not even retry a busy share -- it trips `UTIL_ASSERT(FALSE)` and answers **-2**. Only PCInStore caches the game pointer; AnyPCOnMap caches only the member count and re-reads the character id at every single use; ItemIsIdentified is the only one that copies the search name out of the trigger ONCE. `NumItemsParty` releases by CHARACTER ID where `NumItems` beside it releases by `pObject->GetId()`. Findings beyond the arms: **`GlobalTimerNotExpired` is NOT the negation of `GlobalTimerExpired`** -- both compare strictly, from opposite sides, so a timer standing exactly at the current time is neither, and a missing variable is TRUE for Expired and FALSE for the other three; the four timer arms each carry their OWN copy of a scope walk that is not the Global family's (scope in String2, upper-cased, cut to SIX characters, no null-check on `m_pArea`). **`CurrentAreaIs` does not compare the area by name at all** -- it scans the res ref for the first DIGIT and compares the number starting there. **`OnIsland` is area-specific with the island's outline hard-wired as three x/y steps, and its default is TRUE**, including for a dead creature, which it tests twice against the same STATE_DEAD bit -- once in the CRE's stored state, once in the derived one. `Frame` SORTS its two bounds instead of assuming an order. `ChargeCount` and `CheckPartyAverageLevel` are each a TRIPLE folded into one opcode, selected by GetInt2 and GetInt1 respectively (1 equal, 2 less, 3 greater). `CheckPartyLevel` and `PartyLevelVS` sum the stored level BYTE while `CheckPartyAverageLevel` sums `GetClassMaskLevel(0xFFF)`, and `PartyLevelVS` never reads its own value -- it compares the party average against the resolved OBJECT's level. **The Reaction triple is the only trigger in the switch with a SIDE EFFECT**: all three post the score through `CGameSprite::FeedBack(FEEDBACK_29, ...)` before answering. In `GetReaction` the hated-race penalty hands `IsHatedRace` this object's own base stats at +0x5A4, past the end of `CGameAIBase`, so the `GetObjectType() == TYPE_SPRITE` test guarding it is load-bearing. Runtime: one `auto_start_game` Prologue run, verdict **PASS / 15 steps**, no crash log, game killed to 0 processes -- **a no-regression result, not proof of the new code**, since s34/s35 already measured that an idle-or-walked Prologue evaluates only fifteen ids and none of the 29 new arms is among them. **A first attempt timed out at 400 s with "no world activation" and the process still alive: the Debug exe has grown and now needs 600.** Sweep deliberately not re-run: only this function moved on the switch side and it is still short of 160. |
| 35 | `s35` | 2026-08-30 | yes | **`CGameAIBase::EvaluateStatusTrigger` went from 76 arms to 111 of 160 -- 113 of the 164 opcodes now have a body, and 51 are left.** Eight verified batches, one commit each, every one parity GREEN, `arc check` PASS and Debug building clean. The map was NOT re-derived: `s34_map.py` and `s34_sizes.py` in `c:\tmp` from s34 still print it, and the size-ranked list is what picked every batch. Recovered: the SummoningLimit and NumCreature triples; five ids TRIGGER.IDS does not name (an area-name compare, the bard-song battle-counter triple, an area-type mask); eight object-resolving predicates (HasWeaponEquiped, CheckDoorFlags, IsRotation, IsFacingSavedRotation, SetLastMarkedObject, SetSpellTarget, IsPathCriticalObject, InTrap); a party-subtracting NumCreature triple plus TimerActive and IsScriptName; the fused spell-state pair, HasItemPersonal and IsPlayerNumber; OpenState, Sequence and IsAnimationID; TimeOfDay and Difficulty; and the NumDead triple. **s34's lesson held every time.** `Sequence` answers for two object kinds and NOT the same way -- a sprite's GetSequence is a SHORT it SIGN-extends, a static's vid-cell id a WORD it ZERO-extends. `IsRotation` sign-extends where `IsFacingSavedRotation` beside it compares SIXTEEN bits wide and takes both operands from the sprite. Two 'triples' run equal / LESS / GREATER, not the usual equal / GT / LT: the battle-song one and the party-subtracting NumCreature one. `SetLastMarkedObject` and `SetSpellTarget` are not predicates at all -- they STORE and return TRUE. `IsScriptName` tests its `_stricmp` for zero and so holds when names MATCH, while the unnamed 0x45739B returns the raw value and so holds when they DIFFER: same helper, opposite polarity. Findings beyond the arms: **`InTrap` passes TYPE_SPRITE where GetObject's parameter is a BOOL `checkBackList`, and answers -2, not FALSE, when nothing resolves**; `IsPathCriticalObject` resolves with GetObject where its neighbours use GetObjectWithType, so nothing checks the object is a sprite before GetBaseStats; `NumDead` concatenates the PREFIX FIRST, so the global it reads is `_DEAD<Name>`; and **`CInfGame::GetFixedOrderCharacterId` was carried as `NOTE: Inlined.` but has a real address, 0x45B610** -- marked and parity GREEN. `TimeOfDay` dispatches through a SECOND jump table at 0x45B1B4; `jumptable_audit` sees only one table in this function and so calls the new source switch UNMATCHED-SOURCE-SWITCH, which is the tool under-reporting, not a source error. Runtime: two `auto_start_game` Prologue runs, PASS/15 steps, no crash log, game killed to 0 processes. An entry log filtered to ids OUTSIDE s34's known fifteen, with a one-shot arm marker to prove the logger was live, produced the arm line and **NOTHING else** -- so walking the party around the Prologue does NOT broaden the id set, and **none of the 36 new arms ran live**. Say it that way. Sweep deliberately not re-run: only this function moved on the switch side and it is still short of 160. |
| 34 | `s34` | 2026-08-30 | yes | **`CGameAIBase::EvaluateStatusTrigger` went from 17 of 160 arms to 76 -- the last actionable arc in the sweep, opened and taken past halfway.** Seven verified batches, one commit each, every one parity GREEN and Debug building clean: HP (equal/LT/percent/lost, plus IsCreatureAreaFlag), the stat comparisons (CheckStat/CheckSkill/LevelInClass/Level), Morale and Happiness, Reputation/PartyGold/NumInParty, the world clock and the die roll, the creature-identity family, and eight state/area predicates. The arc is tractable because the whole opcode->body map was derived up front from the index table at 0x45B0C4 and the jump table at 0x45AE40 and cross-referenced with TRIGGER.IDS: 164 opcodes, 160 distinct bodies, four fused. **The recurring lesson is that same-shaped triples are NOT the same code.** `Morale` compares BYTES and UNSIGNED (`cmp cl, bl` at 0x45479C) while `Happiness` sign-extends a SHORT; `PartyGold` is an unsigned DWORD while `NumInParty` is a signed SHORT; `Level` calls `GetClassMaskLevel(0xFFF)` where `LevelInClass` calls `GetClassLevel`; `RandomNum` reverses the operands (range is GetSpecifics, the tested value is GetInt1); `Class` is not a comparison at all but `IsClassValid`; `Race`/`Specifics` decode the WHOLE trigger and read via `GetCause()` where their neighbours decode only the cause. Three findings beyond the arms: **`HasInnateAbility` is hard-wired FALSE** -- the table sends it to the same `xor esi, esi` as `False`; **`FallenPaladin` gates on class mask `& 7` = BARBARIAN\|BARD\|CLERIC, not the paladin bit**, corroborated by the binary's own 0x600 and 0x122 masks, and only then tests CRE flag bit 9; and **`IsExtendedNight` does NOT null-check `m_pArea` while `AreaRestDisabled` right beside it does**. Runtime: three `auto_start_game` + AutoUI runs of the Prologue, no crash log, game killed to 0 processes each time. An instrumented entry-log proved the function still runs and the DEFAULT still returns FALSE (6177 hits via `Or` 0x4089), but the 15 ids an idle Prologue evaluates are all pre-existing arms -- **the new arms are reachable but not from an idle save** (`HPPercentLT` alone appears 412 times in the compiled scripts). The clean build completes the scenario PASS/6 steps; the second instrumented run stalling at a 4MB debug log was the LOGGER, not the code. Sweep unchanged at CLEAN 438 / MISSING 2 by construction: only this one function moved and it is still short of 160. |
| 33 | `s33` | 2026-08-29 | yes | **`CScreenCharacter::UpdatePopupPanel` dispatched ONE popup of twenty, so every character-screen popup but Information went un-updated -- no Done button re-enabled, no scroll bar re-bound.** The binary's table (0x5E0DC0, index 0x5E0E14, 54 values from 4) has twenty real arms and thirty-two that fall to the default assert; the source had one `case`. Most arms tail-dispatch to an `Update*Panel` that already existed, but FOUR did not, which is what made this an arc rather than a one-liner: 0x5DAF30 `UpdateInformationPanel` was already here INLINED into `case 57` and the binary CALLS it, so it moved out; 0x5E87E0 `UpdateFeatsPanel`, 0x5F7B00 `UpdateSkillsPanel` and 0x5DB200 `UpdateRecordsPanel` are new recoveries. The feats/skills pair is NOT symmetric -- opposite HP save/restore ordering, different enable predicates (`CanSelectFeat && m_nExtraFeats > 0` vs `nCost != 0 && m_nExtraSkillPoints >= nCost`), skills calls `GetSkillId` TWICE in its label loop and once in its button loop, and only skills ends a row with a three-way colour -- so mirroring one onto the other would have shipped a twin swap. `case 20` calls `GetControl(6)` BEFORE testing the panel for NULL, which is the byte order, not a slip. `UpdateRecordsPanel` has THREE CString locals, not four: the epilogue has exactly three destructors and the time string and favourite-spell name are the same slot. Cases 9/10/50 are real empty arms that jump past the assert. RUNTIME-PROVEN: portrait -> `goto character` -> click opened panels 57 and 13 (`active:0 -> 1`) with no crash, which is what mattered because the faithful `UTIL_ASSERT(FALSE)` default is newly fatal where the old default was a bare `break;`. jumptable_audit 0x5E0B20 CLEAN (20 arms, was 1); parity GREEN on all five. Sweep 515: CLEAN 437 -> 438, MISSING 3 -> 2, everything else unchanged. TRIGGER.IDS recon for the last big arc: 182 names 0x400A..0x40F8, base 16394 = 0x400A = `Alignment`, and exactly ONE duplicated id in range (0x4039, a harmless alias). |
| 32 | `s32` | 2026-08-29 | yes | **The sweep's last three INCOMPLETE arms are gone, and so is every DEFAULT: CLEAN 423 -> 437, MISSING 34 -> 3, INCOMPLETE 3 -> 0, DEFAULT 9 -> 0, OUT-OF-RANGE 24 -> 10.** Four real recoveries. `CUIControlEdit::OnKeyDown` held all three INCOMPLETE -- the DBCS bodies of Backspace, Return's history shift and Left -- plus a missing `VK_ESCAPE` arm that restores `m_sOriginalText` and kills capture unconditionally. `CUIControlButtonInventorySlot::Render` never named an empty slot: its fused table sets a STRREF beside each STON* placeholder, and the single exit passes it to `SetToolTipStrRef` where we passed -1. Runtime-proven live -- 22 and 23 report "Left Ring" and "Right Ring", so the SPLIT was real, and the three slots holding items show the ITEM's description. `CAIObjectType::OfType` handled the eleven single classes and none of CLASS.IDS' eight groups, so `Class(x, MAGE_ALL)` matched nothing; the masks name them 8 for 8 against a rival BG2-era sequence. Plus `OnActionButton`'s `case -1` on a BYTE (dead code; the binary means 255) and a keymap arm. Three more false-positive classes (20-22) cleared 28 findings across four functions with NO source change: a reloaded index register carries no bias, a class-scope `const CClass::NAME = OTHER;` has to resolve, and the right inlined callee among namesakes is the one that contradicts the table nowhere. |
| 31 | `s31` | 2026-08-17 | yes | **The party-move bug the user reported is fixed and runtime-proven.** All three `CAIGroup` formation entry points asked for `GetArea(0)` in sixteen places where the binary asks for two other things: `GetVisibleArea()` for the search add/remove, and the SPRITE'S OWN `m_pArea` for `AdjustTarget`. `m_visibleArea` is 0 only until the party first transitions, which is exactly why the bug appeared after an area change and never with one character selected — that path calls `AdjustTarget` at all. A live trace caught both states in one run: `area0 == visible` before the transition, `area0` still the OLD area after it. Also `CResBIO` (AllocResObject's last three arms, name taken from BG2's `CResHelper<CResBIO,1022>`), three real switch arms in the inventory/create-char screens, and four more `jumptable_audit` false-positive classes (16–19): CLEAN 392 → 423, MISSING 71 → 34, twelve functions cleared, three newly visible. |
| 30 | `s30` | 2026-08-16 | yes | `jumptable_audit` now DERIVES the table↔switch pairing: AMBIGUOUS-PAIRING 181 → 59, CLEAN 268 → 392, OUT-OF-RANGE 53 → 13. Five more false-positive classes (11–15), the largest being that an inlined callee's table is diffed against the callee's own recovery via `// NOTE: Uninline.`. Two real defects out of the UI triage: `CScreenWorld::CancelPopup` called `StopDeath` where the binary calls nothing (and `CancelEngine` did it for case 15, which asserts) — both missing `case 22`; and icon index 20 had no arm in `CGameDoor::SetCursor` or `CGameTrigger::SetCursor`. All four parity GREEN. |
| 29 | `s29` | 2026-08-16 | yes | The sweep's only INCOMPLETE was the tool's own bug, not a second Power Attack. Three more false-positive classes fixed (case-group brace scoping, Ghidra's end vs *undefined* functions, C escape literals). One real finding: `CGameAnimationTypeMonster::SetSequence` sequences 8–13 hit the `default:` assert — recovered — plus an `m_currentVidCellWeaponBase` member swap it exposed. First live proofs of `CGameDoor::CompressTime` (5 of 6 doors, sub-hour delta closed none) and `CheckModal` case 0 (bard song). Sweep 522/28 → 515/24. |
| 28 | `s28` | 2026-08-16 | yes | `CheckFeatPrerequisites` COMPLETE (MERCANTILE_BACKGROUND, SNAKE_BLOOD were the last two stubs). New tools `jumptable_audit.py` (seven false-positive classes) and `save_party.py`. Both ported SUMMON blocks runtime-proven with a sprite census. |

Sessions before 28 are not numbered here — their record lives in the
`iwd2-re-combat-gap-archive` memory file (UPDATE 1–43). Do not backfill this
table by guessing which commits belonged to which session; the mapping was
never recorded and several sessions share a day.

## Ending a session

1. Add the row above, newest first, with the `pushed` column set honestly
   (`git log --oneline origin/main..HEAD`).
2. Commit it — that commit is the session's last.
3. `git tag -a sNN -m "<one-line headline>"` on it.
4. Update the `iwd2-re-combat-gap` memory's `description:` so it names the same
   session number.

Tags are local until pushed: `git push origin sNN` (or `git push --tags`).
