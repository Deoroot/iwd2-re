# Diagnose the empty save menu. Options is reachable from the world via the
# left panel (CBaldurEngine::OnLeftPanelButtonClick, 9 = options); the save
# screen hangs off it.
wait world 3000
waitgo 6000
sleep 30
dump
goto options
sleep 60
screen
dump
