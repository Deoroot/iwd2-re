# Open the container store, then leave it with ESCAPE. CScreenStore::OnKeyDown
# routes VK_ESCAPE to SelectEngine(world) + CScreenWorld::StopStore, which is
# what restarts world time and clears the host store permission.
#
# Same save as store-open-bag:
#   scripts/vm.sh smoke --save "000000039-store" 120 \
#       --ui scripts/scenarios/store-close-esc.txt --hit CScreenWorld::StopStore
wait world 3000
waitgo 6000
sleep 30
goto inventory
sleep 120
expect screen inventory
rclick 2 30
sleep 60
click 5 9
sleep 240
expect screen store
key 27
sleep 240
expect screen world
