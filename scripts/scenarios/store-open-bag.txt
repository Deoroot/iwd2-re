# Open the party inventory and dump its controls, so the Bag of Holding
# slot can be addressed by (panel, control) instead of by pixels.
wait world 3000
waitgo 6000
sleep 30
goto inventory
sleep 120
expect screen inventory
dump
