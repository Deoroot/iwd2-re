# Prove CScreenSave::RefreshGameSlots runs and the save menu is populated.
# World -> options (left panel button 9) -> the options "Save" control, which
# is CScreenOptions.cpp's m_nID 6 on panel 2/13 and calls CScreenSave::StartSave.
# StartSave is what refreshes the slot list, so `dump` on the save screen shows
# whether the list came back with controls.
wait world 3000
waitgo 6000
sleep 30
goto options
sleep 60
dump
click 2 6
sleep 90
screen
dump
